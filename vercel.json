{
  "version": 2,
  "builds": [
    { "src": "status-server.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "status-server.js" },
    { "src": "/(.*)", "dest": "/public/$1" }
  ]
}
